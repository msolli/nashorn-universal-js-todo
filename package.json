{
  "name": "universal-js-todo",
  "version": "1.0.0",
  "description": "Universal JS Todo Example",
  "private": "true",
  "scripts": {
    "build": "npm run build:js",
    "build:js": "npm run build:js:client",
    "build:js:client": "webpack --config webpack.client-config.js",
    "build:js:server": "webpack --config webpack.server-config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Martin Solli",
  "license": "MIT",
  "dependencies": {
    "webpack": "^1.11.0"
  },
  "devDependencies": {
    "babel-core": "^5.8.22",
    "babel-loader": "^5.3.2"
  }
}
